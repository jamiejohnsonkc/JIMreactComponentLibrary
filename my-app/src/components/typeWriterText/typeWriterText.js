import React from "react";
import PropTypes from "prop-types";

export const typeWriterText = (children, ...props) => <h1>{children}</h1>;

// typeWriterText.propTypes = {};

// typeWriterText.defaultProps = {};

export default typeWriterText;
